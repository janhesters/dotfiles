# Espanso reads this profile through ~/.config/espanso/config/default.yml.
# The selector symlink is switched at runtime; this file remains Stow-owned.

toggle_key: OFF
search_shortcut: ALT+SHIFT+SPACE
auto_restart: false

keyboard_layout:
  layout: us
  variant: ""
  options: compose:paus
